# Render env vars
NODE_ENV=production
PORT=5050
MONGO_URI=
JWT_SECRET=
# Comma-separated list of allowed origins for CORS (include your Netlify URL)
# Example: https://liftlog.netlify.app,https://liftlog-xyz.netlify.app
CORS_ORIGINS=
# Optional
MAX_SIGNUPS=10
ADMIN_SECRET_KEY=
